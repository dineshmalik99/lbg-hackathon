import React from 'react';
import { Typography } from '@mui/material';

const History = () => {
  return (
    <Typography variant="h4">Contact & Audit History</Typography>
  );
};

export default History;